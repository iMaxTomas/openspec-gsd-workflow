## 1. Freeze independent OpenCode scope

- [ ] 1.1 Record this change as an independent OpenCode-local multiagent planning line.
- [ ] 1.2 Record that this change does not replace the accepted broader multi-runtime mainline.
- [ ] 1.3 Record that the immediate target is fast local OpenCode landing, not lab-wide topology rewrite.

## 2. Freeze the v3 team shape

- [ ] 2.1 Define one front-door owner as the only operator-visible closure point.
- [ ] 2.2 Define the retained specialist classes as execution, research, and review lanes.
- [ ] 2.3 Record that specialist lanes are on-demand helpers rather than permanent peer owners.
- [ ] 2.4 Record that current model bindings are transitional and not architectural truth.

## 3. Freeze the hard gate routing contract

- [ ] 3.1 Define research-lane entry conditions for read-heavy, discovery, comparison, and inspection work.
- [ ] 3.2 Define execution-lane entry conditions for bounded write or bounded implementation work.
- [ ] 3.3 Define review-lane entry conditions for high-risk, high-cost, disagreement, and final-judgment work.
- [ ] 3.4 Define the `observation-only` rule for slice-external results and the correction/promotion gate needed before formal adoption.
- [ ] 3.5 Define threshold terms for `high-risk`, `high-cost`, and `materially conflicting` so route selection is inspectable.

## 4. Freeze the passive state pipeline

- [ ] 4.1 Define the passive path as a pipeline rather than a hidden internal team.
- [ ] 4.2 Define the allowed inputs as evidence only.
- [ ] 4.3 Define the allowed outputs as logs, checkpoints, unresolved items, and handoff candidates only.
- [ ] 4.4 Define the prohibition on routing, interrupting, or speaking as an operator-visible authority.
- [ ] 4.5 Define minimum artifact schemas for `state-log`, `checkpoint`, `unresolved-item`, and `handoff-candidate`.
- [ ] 4.6 Define passive cadence and output surface boundaries.

## 5. Freeze owner and promotion contracts

- [ ] 5.1 Define the owner contract for boundary restatement, lane declaration, and final answer classification.
- [ ] 5.2 Define the `observation-only` promotion workflow as candidate -> review/correction -> promotion -> preserved lineage.
- [ ] 5.3 Define when review-lane promotion is mandatory versus when owner correction is sufficient.

## 6. Prepare later implementation handoff

- [ ] 6.1 Define the implementation boundary for `opencode.json` and `.opencode/agents/`.
- [ ] 6.2 Define the implementation boundary for passive logging or checkpoint plugin wiring.
- [ ] 6.3 Define the first implementation slice so later work can start without reopening the planning line.
- [ ] 6.4 Freeze slice one to one custom primary front-door owner.
- [ ] 6.5 Freeze slice one to visible execution, research, and review lanes.
- [ ] 6.6 Freeze slice one passive outputs to `state-log` and `checkpoint` only.

## 7. Add planning-time validation gates

- [ ] 7.1 Define a shadow-mode validation pass for the passive pipeline before any operator-visible adoption.
- [ ] 7.2 Define a non-interference proof for passive outputs versus front-door owner answers.
- [ ] 7.3 Define a duplicate-routing and duplicate-handoff review pass.
- [ ] 7.4 Define rollback conditions if the passive pipeline becomes noisy or role boundaries drift.
